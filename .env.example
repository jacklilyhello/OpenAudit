# Development values only. Use real production keys from your secret manager and never commit them.
OPENAUDIT_ENV=development
OPENAUDIT_CONFIG=./config.example.yml
OPENAUDIT_API_KEYS=dev-key
OPENAUDIT_ADMIN_API_KEY=dev-admin-key
OPENAUDIT_ALLOW_UNSAFE_PRODUCTION=false
